import { useContext } from "react";
import { AuthContext } from "../context/AuthContext";

// Re-export for convenience — use this instead of importing useAuth from context
export { useAuth } from "../context/AuthContext";